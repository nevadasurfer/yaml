apiVersion: v1
kind: Service
metadata:
  name: {{ include "mygitea.fullname" . }}
  labels:
    {{- include "mygitea.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mygitea.selectorLabels" . | nindent 4 }}
